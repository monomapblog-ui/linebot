# YOIN DESK プロジェクト状況メモ

運営: 合同会社グラステ（yoinと同じ会社）
事業内容: メンズエステ専門「24時間受付代行サービス」（LINE/電話問い合わせの一次対応を代行。既存の予約管理システム（Caskan等）とは競合せず、その手前の一次対応を補完するポジション）

## 現在の状態

`lp/` 配下にランディングページ一式を実装・プッシュ済み（コミット `3a27008`）。

- `lp/index.html` … トップページ（LP本体）。ヒーローに「深夜の対応ログが流れるコンソール風パネル」の演出あり
- `lp/company.html` … 運営会社情報
- `lp/privacy.html` … プライバシーポリシー（通話録音・エンドユーザー個人情報の取り扱いを想定）
- `lp/terms.html` … 利用規約（BtoB契約、免責範囲、解約条件など）
- `lp/contact.html` … お問い合わせフォーム（HTML5ネイティブバリデーション実装済み、**送信先バックエンド未接続**。現状は検証成功時に`thanks.html`へ遷移するだけ）
- `lp/thanks.html` … 送信完了ページ
- `lp/style.css` … 全ページ共通スタイル（黒・ダークネイビー・ゴールドの配色、数字はモノスペース体で統一）

デザイン意図: Tailwind CDNは本番非推奨のため使わず、依存なしの素のCSSで実装。

## 定款について（重要・本人合意済みの判断）

合同会社グラステの定款はイベント・エンタメ・飲食店運営中心の文言で、この「受付代行（BPO/コールセンター業務）」は**現状の14項目のどれにも該当しない**（yoinの時と違い、こじつけでカバーできる項目がない）。

本人と協議の結果、**今は定款変更をせずにそのまま事業を開始する方針**で合意済み（2026年7月時点）。ただし以下のタイミングまでには定款変更（目的追加登記、登録免許税3万円程度、自分で法務局手続き可能）を済ませることを推奨として伝えてある：

- 最初の実契約が決まりそうなタイミング
- このサービス用にStripe等の決済・本人確認を進めるタイミング

追加すべき文言案（未実施）:
```
電話・オンラインチャット等による受付代行、コールセンター業務の企画、運営及び受託
```

## 残っている作業

1. **お問い合わせフォームのバックエンド実装**（未着手）— Next.jsで作るのが良さそう。yoinと同じ構成に寄せるか検討
2. LINEボットの実装本体（当初の構想。Dify等のノーコードAIチャットボット構築ツールでLINE Messaging API連携するプランだったが、方向性が「受付代行サービス」の訴求LPに寄ったため、実際のボット/オペレーション部分は未着手）
3. 料金プランの数字（¥38,000〜、¥78,000〜等）は仮の数字。実際の金額が決まったら`lp/index.html`の料金セクションを更新
4. 上記の定款変更（本人が任意のタイミングで実施）
