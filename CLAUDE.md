# YOIN DESK プロジェクト状況メモ

運営: 合同会社グラステ（yoinと同じ会社）

## 事業内容（2プラン構成）

メンズエステ専門「受付代行サービス」。既存の予約管理システム（Caskan等）とは競合せず、その手前の一次対応を補完するポジション。単独でも組み合わせでも導入可能な2プランで展開:

1. **LINE自動受付プラン**（AI・ソフトウェア） — LINE公式アカウントのメッセージにAIが24時間自動応答。コード側で実装可能・実装済み
2. **電話受け代行プラン**（有人BPO） — 専門オペレーターが電話に直接応対。実際に人を採用または既存コールセンターBPO会社と提携する必要があり、コードでは作れない事業運営そのものの話（未着手、本人の事業判断待ち）

## 現在の状態（すべてリポジトリルート直下、コミット `8b256e6`）

**静的サイト**（Vercelにゼロコンフィグでデプロイ済み、プロジェクト名 `yoin-desk`）
- `index.html` … トップページ（LP）。2プランを並べた構成、料金は相場の10%引きで設定済み
- `company.html` / `privacy.html` / `terms.html` / `contact.html` / `thanks.html`
- `style.css` … 全ページ共通スタイル（黒・ダークネイビー・ゴールド、数字はモノスペース体）
- Tailwind CDNは本番非推奨のため使わず、依存なしの素のCSSで実装

**バックエンド（Vercel Serverless Functions）**
- `api/contact.js` … お問い合わせフォームの送信先。Resend経由でメール通知。**動作確認済み**（本人が実際に送信・受信を確認）
- `api/line-webhook.js` … LINE自動受付プランの本体。LINE Messaging APIのWebhookを受け、Claude（Haiku）で店舗のFAQ設定（`lib/salon-config.js`）に基づいて自動応答。署名検証・エラー時のgraceful fallbackをローカルで検証済み。**実際のLINE公式アカウントとの接続はまだ未実施**（LINEチャネルシークレット・アクセストークン・ANTHROPIC_API_KEYが必要）
- `lib/salon-config.js` … デモ店舗1店舗分のFAQ情報をハードコード。複数店舗展開時はチャネルIDごとに設定を切り替える設計に拡張が必要

## 料金（相場調査済み、意図的に相場の10%引き）

| プラン | ライト | スタンダード |
|---|---|---|
| LINE自動受付 | ¥29,000/月〜 | ¥49,000/月〜 |
| 電話受け代行 | ¥37,000/月〜 | ¥67,000/月〜 |

根拠: 電話代行(24h対応)は相場月4〜8万円、生成AIチャットボットは中価格帯月3〜15万円（WebSearchで調査済み、出典はセッション内会話に記載）。複数店舗・両プラン併用は個別見積もり。

## 定款について（重要・本人合意済みの判断）

合同会社グラステの定款はイベント・エンタメ・飲食店運営中心の文言で、この「受付代行（BPO/コールセンター業務）」は**現状の14項目のどれにも該当しない**（yoinの時と違い、こじつけでカバーできる項目がない）。

本人と協議の結果、**今は定款変更をせずにそのまま事業を開始する方針**で合意済み（2026年7月時点）。ただし以下のタイミングまでには定款変更（目的追加登記、登録免許税3万円程度、自分で法務局手続き可能）を済ませることを推奨として伝えてある：

- 最初の実契約が決まりそうなタイミング
- このサービス用にStripe等の決済・本人確認を進めるタイミング

追加すべき文言案（未実施）:
```
電話・オンラインチャット等による受付代行、コールセンター業務の企画、運営及び受託
```

## 残っている作業

1. **LINE公式アカウントとの実接続**: LINE Developersでチャネル作成 → `LINE_CHANNEL_SECRET` / `LINE_CHANNEL_ACCESS_TOKEN` をVercelに設定 → Webhook URL（`https://<デプロイURL>/api/line-webhook`）を登録
2. **`ANTHROPIC_API_KEY`をVercelに設定**（LINEボットの応答生成に必要）
3. **複数店舗対応の設計**: 現状`lib/salon-config.js`は1店舗分のみ。実際に複数店舗へ展開する際は、LINEチャネルIDごとに設定を出し分ける仕組みが必要
4. **電話受け代行プランの実体づくり**（本人の事業判断・オペレーション構築、コードでは解決しない）
5. 上記の定款変更（本人が任意のタイミングで実施）
