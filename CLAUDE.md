# YOIN DESK プロジェクト状況メモ

運営: 合同会社グラステ（yoinと同じ会社）

## 事業内容（2プラン＋経営サポートオプション）

メンズエステ専門「受付代行サービス」。既存の予約管理システム（Caskan等）とは競合せず、その手前の一次対応を補完するポジション。単独でも組み合わせでも導入可能な2プランで展開:

1. **LINE自動受付プラン**（AI・ソフトウェア） — LINE公式アカウントのメッセージにAIが24時間自動応答。コード側で実装可能・実装済み（アーキテクチャの詳細・重要な制約は下記「LINE自動受付プランのアーキテクチャ方針」参照）
2. **電話受け代行プラン**（有人BPO） — 専門オペレーターが電話に直接応対。実際に人を採用または既存コールセンターBPO会社と提携する必要があり、コードでは作れない事業運営そのものの話（未着手、本人の事業判断待ち）

## LINE自動受付プランのアーキテクチャ方針（2026年9月8日確定）

本人の構想: 「WEB予約システムと連動して、人間が打ち込んで確定するような形をAIが自動で代替する」イメージ。契約店舗ごとに少しカスタマイズして提供したいとの希望。電話受付と並行運用する前提（LINE自動受付が電話受け代行を置き換えるわけではない）。

**判明した制約**: 主要な対象システムであるメンズエステ専用の統合予約・管理ツール「Caskan（キャスカン）」は、**公式の標準機能として外部向けAPI連携を提供していない**（本人確認済み）。そのため「AIが予約システムに直接書き込んで確定する」完全自動化は、少なくとも現時点では技術的に選べない。

**本人と合意した方針**: 完全自動化は目指さず、**「AIが予約内容の整形・引き継ぎまでを担い、最終確定は人間が行う」**半自動アーキテクチャを採用する。

- LINEでのやり取りをAIが処理し、コース・希望日時・お客様情報など必要事項を構造化して整形する
- 整形した内容を、店舗スタッフまたは電話受け代行プランの自社オペレーターへ引き継ぐ（既存の電話受け代行プランの「対応内容は記録のうえ店舗へ引き継ぐ」フローと同じ形に統合できる）
- 実際にCaskan等の予約システムへの最終入力（確定操作）は、引き続き人間が行う
- 店舗ごとのカスタマイズは、この「整形・引き継ぎ」の内容（FAQ設定・引き継ぎ先・使用する予約システムの用語等）を`lib/salon-config.js`的な設定ファイルで店舗ごとに切り替える形で対応可能（既存の設計方針と整合）
- **今後の拡張余地**: Caskan側にパートナー向けの非公開API・CSV/メール連携等がないか、別途問い合わせを検討する余地はある（本人が直接打診するかは未定）。取得できれば将来的に真の自動確定へ発展させられるが、現時点の実装方針はこれに依存しない

**理由**: (1) 画面操作の自動化（RPA）はCaskan側の仕様変更で頻繁に壊れる・利用規約上グレーになりやすいためベースにしない、(2) 半自動方式は既存の電話受け代行プランの人的引き継ぎフローをほぼそのまま流用でき、Caskan側の協力なしに今すぐ実装できる、という現実的な理由から選択。

**経営サポートオプション**（2026年7月追加）: 電話受け代行プランに組み合わせられる事務代行オプション（女子給精算代行・シフト作成管理・SNS更新代行・広告更新代行）。本人がメンエススタッフとして就業した経験があり、精算方法等を理解しているため、**実際に今すぐ提供する前提**で追加（LINE自動受付プランのような「近日公開」ではなく、電話受け代行プランと同じく現時点で実施可能な扱い）。競合「クラウド店長（Parallel）」の実際の料金ページ（本人がスクリーンショットで共有）を基準に、10%引き程度で価格設定

**女子給精算代行の実物サンプル検討（2026年8月4日）**: 本人が実際に就業している店舗の受付表（Googleスプレッドシート由来のxlsx）を、本人が匿名化した上で共有（`仮.xlsx`、リポジトリ直下、コミット`b5f5a83`）。構造・関数を確認し、機密漏洩がないか点検済み:
- シート構成: 予約ログ表（B〜S列、5〜104行=1日分100枠）＋出勤・精算表（V〜AD列）の2表構成。関数は`IFERROR`での安全な減算、`SUMIF`によるセラピスト別集計、LINE送信用の精算メッセージをセル内で組み立てる長い文字列結合式など
- `K`列の`XLOOKUP`が参照する`'店落ち'`シート（コース×セラピスト別の歩合率マスタ）は、この単票のブックには含まれていなかったが、本人確認により**オプションはフルバック（店落ちなし）のため店落ちは実質コース区分ごとの固定額**と判明。個別セラピストごとのマスタ表は不要
- 点検結果: 電話番号・住所・URL・ドキュメントメタデータ（作成者情報）などの直接的な個人情報・機微情報は**検出されず**。セラピスト名（列E）はメンエス業界の源氏名らしい名前だが本人の説明通り架空、客名（列F）はA/B/C等の明確なプレースホルダー、ルーム名も英字1文字（M/P/S等）のみで実在店舗の特定情報は含まれていない
- **本人からの明示的な指示（2026年8月4日）**: 「スプシに関しては記憶しておいてほしい　仮名やルームAのように名前変えて」— 今後このデータに言及・参照する際は、常に仮名（架空の源氏名）・ルームA方式のような架空ラベルを用いること。将来類似データを扱う際も同様に徹底する
- `仮.xlsx`は構造抽出完了後、本人の希望により**リポジトリから削除済み**（コミット`c7241b1`）
- 上記構造をもとに、完全架空データの女子給精算サンプルシート（`予約ログ`→`出退勤・精算`が自動連動、店落ちはコース区分ごとの固定レート表、精算メッセージ自動生成）を作成し、本人にファイル納品済み（生成スクリプトはセッションのスクラッチパードのみ・リポジトリ非管理）
- **重要な教訓（同日中、別ファイル再アップロード時に発覚）**: 本人が「もっと忠実に再現したい」として同種の受付表（`仮.xlsx`と同一内容、`/root/.claude/uploads/`配下に一時アップロード）を再送してきた際、T列（予約確定メッセージ生成式）とU列（出張先情報）に**実在する神戸・三宮/北野エリアのマンション名＋号室が生データのまま残っていた**ことが発覚（セラピスト名・ルーム記号は本人の説明通り架空化されていたが、この列は未対応だった）。本人に確認の上、即座にアップロード元ファイルを削除（本人了承済み）。**教訓**: 「架空化した」という本人申告があっても、隠れた列・自動生成メッセージ内の埋め込み文字列（住所・建物名等）まで含めて機械的に全列スキャンして確認すること。目に見える主要列（名前・ルーム記号など）だけで判断しない。次回このテーマのデータを受け取る際は、渡された直後に全列・全セルへの正規表現スキャン（住所キーワード・建物名パターン等）を最初に行う
- **その後、本人が「危ない情報を全て削除した」第3のファイルを再送**（同じく`/root/.claude/uploads/`に一時アップロード）。受け取り直後に全列・全セルへ正規表現スキャン（住所キーワード・電話番号・URL・メール）を実施し、ヒットなしを確認。ルーム列は「ルームA」等に、セラピスト名列は空欄に、それぞれ本人が事前に置き換え済みだったことも確認。この確認を経て、入力規則10個（新規/会員・種別F/A/B・コース・値引き種別・店舗・予約経路・出勤ステータス等）を含めて構造を完全再現したテンプレート（`yoin-desk-seisan-template-v3.xlsx`、K/L/GALの実際のXLOOKUP構造・予約確定メッセージの自動生成ロジックまで再現、会場のみ仮のルーム名）を作成し納品済み。**このアップロードファイルも用が済み次第、本人の許可を得た上で削除するのではなく、確認完了後に自主的に削除**（今後もアップロードされた元ファイルは、テンプレート抽出が終わり次第、都度セッション内から削除する運用とする）

## ブログ（2026年8月4日追加）

営業の反応が薄いことを受け、コールドDM/メール以外の**受け身（インバウンド）の集客チャネル**として`blog/`配下にブログを追加。競合ナイトデスク（nightdesk.jp）にブログ機能があったのを見て本人が着想。

- `blog/index.html` … 一覧ページ
- `blog/phone-support-mistakes.html` … 第1弾記事「メンズエステの電話対応、よくある3つの課題と対策」。Problemセクションの悩みを実用的なハウツー記事に展開し、最後に自然な形でCTAへ誘導する構成（売り込み一辺倒にしない）
- **重要**: ブログページは`company.html`等と違って`<meta name="robots" content="noindex">`を**意図的に付けていない**（検索エンジンに拾ってもらうのが目的のため）。新しい記事を追加する際、他の定型ページをコピーして作ると誤ってnoindexを引き継いでしまうので注意
- 全ページのヘッダーnav・フッターに「ブログ」リンクを追加済み
- 今後の記事追加は、このリポジトリのCLAUDE.mdやセッション内の「あるある」投稿ネタなどを、より実用的なハウツー記事に転用していく方針が考えられる（未着手）

## セッション環境について（注意）

このプロジェクトの作業ディレクトリは**セッションのコンテナが再起動されると消える**（2026年8月4日・9月8日に発生、`add_repo`→再クローン→`register_repo_root`で復旧）。**作業ディレクトリのパスは毎回同じとは限らない**（`/workspace/linebot`だったこともあれば`/home/user/linebot`だったこともある）。作業再開時にCLAUDE.mdで記憶しているパスが存在しない場合は、`add_repo`のレスポンスが返す実際のワークスペースパスを確認したうえで、この手順を再度行うこと。

## 現在の状態（すべてリポジトリルート直下）

**本番URL: `https://linebot-iota-ashy.vercel.app`**（Vercelプロジェクト名 `linebot`）

**静的サイト**（Vercelにゼロコンフィグでデプロイ済み）
- `index.html` … トップページ（LP）。電話受け代行プランを先頭に据えた構成（LINE自動受付プランは経営サポートオプションの隣に「近日公開」表示。詳細は下記「料金」参照）
- `company.html` … 会社概要ページ。単なる情報テーブルではなく、MESSAGE/POSITION/TRUST等を含む「about」的な構成（下記「2026年9月22日 デザインリニューアル」参照）
- `privacy.html` / `terms.html` / `thanks.html`
- `contact.html` … **2026年9月22日、`index.html#contact`へのリダイレクトページに変更**。実フォームは`index.html`に統合済み（重複メンテナンス防止のため）
- `style.css` … 全ページ共通スタイル。**2026年9月22日、デザインを全面刷新**（詳細は下記「2026年9月22日 デザインリニューアル」参照。旧ハイブリッド配色の記録は本セクション末尾に残すが、現行実装ではない）
- Tailwind CDNは本番非推奨のため使わず、依存なしの素のCSSで実装

## 2026年9月22日 デザインリニューアル（現行デザイン）

本人がClaude Design（`claude.ai/design`）でTOP/ABOUTページのhigh-fidelityデザインを作成し、`DESIGN.md`・`CONTENTS.md`・`README.md`（デザイン仕様）・`YOIN DESK TOP.dc.html`・`YOIN DESK ABOUT.dc.html`をアップロードして「README.mdの仕様通りに実装して」と依頼。README.mdの指示（"`.dc.html`はデザインリファレンスであり、対象コードベースの既存パターンで作り直すこと"）に従い、インラインスタイルの`.dc.html`をそのまま使わず、`style.css`のクラス設計として全面的に書き直した。

**配色（旧ゴールド×ネイビーの高級感路線から刷新）**:
- Primary Blue `#1E4FD6`（信頼・リンク）、Gold `#C9A24B`（CTA・アクセント、旧ブランドから継承）、Navy `#0F1730`（ダークセクション）、白背景中心
- 旧`.section-light`によるライト/ダーク切り替え方式は廃止。新設計ではセクション単位で`.section-dark`等の明示クラスを都度指定する素直な構造に変更

**主な変更点**:
- `index.html`: ヒーロー・課題・サービス（2カード：電話受け代行＝ライト、経営サポートオプション＝ダーク。**LINE自動受付プランは現時点で非表示**、README指示通り）・料金・比較・信頼・導入の流れ・FAQ・お問い合わせ（フォームをTOPページに統合）・フッターの構成に全面刷新
- `company.html`: 単純な会社概要テーブルから、MESSAGE（ブランドメッセージ）・POSITION（Caskan等との非競合を図解）・TRUST・COMPANY・CTAを含む「about」ページに刷新
- FAQは`<details>`から、単一開閉（同時に1つだけ開く。README仕様）のボタン+JS実装に変更
- `contact.html`は`index.html#contact`へのリダイレクトのみに縮小（フォーム二重管理を避けるため）。フィールド名・送信先API（`/api/contact`）は変更なし
- `privacy.html` / `terms.html` / `thanks.html` / `blog/*` はヘッダー・フッター・パンくずのみ新デザインに合わせて更新（本文コンテンツは無変更）
- レスポンシブ対応はREADME.mdに「未対応」と明記されていたため、本人の指示なしにこちらの判断でREADME自身が提案する方針（2カラム→1カラム、比較表→カード化等）に沿って実装
- Playwrightで全ページ・デスクトップ/モバイル双方のスクリーンショットを取得し、リファレンスの`.dc.html`と目視比較して一致を確認済み

- **ヒーロー画像は本人が再アップロードし、`assets/hero-operator.jpg`として反映済み**（2026年9月23日）。1536×1024のオリジナルから4:5縦長にトリミング・800px幅にリサイズ（77KB）。拡張子はデザイン仕様の`.png`ではなく実体に合わせて`.jpg`に（`index.html`側のパスも合わせて修正済み）
- **モバイル表示の読みやすさ改善（2026年9月23日）**: 本人から「PCは見やすいがモバイルが見づらい」とのフィードバック。実機を模したPlaywrightスクリーンショットで確認したところ、ヒーローセクションでモバイル時のみ`order:-1`によりヒーロー写真が見出し・リード文・CTAボタンより先に表示され、価値訴求に到達するまでの縦スクロール量が過大になっていた。`order:-1`を削除してテキスト→写真の自然な順序に変更し、写真サイズも縮小（`max-width: 220px`）。CTAボタンをモバイルでは全幅化してタップしやすく調整。モバイル用クイックナビ（横スクロールピル）の右端に「まだ続きがある」ことを示すフェードのヒントを追加

**未解決・要本人対応**:
- OGP画像・favicon未設定（README.mdでも「未確定・要判断の事項」として明記されていた）

## 配色（旧ハイブリッド light/dark、2026年7月末・現在は上記2026年9月22日の刷新で置き換え済み・履歴として保持）

「全面黒だと暗い印象・胡散臭さがある」という本人指摘を受け、`index.html`のセクションを白背景中心＋一部黒背景のハイブリッド構成に変更。

- **白/クリーム系（`.section-light`クラス）**: Hero・Problem・Benefit・Pricing・Flow・FAQ・**CTA最終**（本人の指摘で追加統一） — 信頼感・情報の読みやすさ重視
- **黒/ネイビー系（変更なし）**: Solution（こだわり部分）・Trust（ブランドの強み） — 高級感の演出用に維持

**実装方式**: 既存コンポーネント（カード・表・バッジ等）はほぼ全て`var(--surface)` `var(--text)` `var(--gold)`等のCSSカスタムプロパティ経由で色を指定していたため、個々のコンポーネントのCSSは書き換えず、`.section-light`クラスでこれらの変数をセクション単位で上書きする設計にした（保守性重視）。ただし以下は個別対応:
- `--gold` / `--gold-soft` / `--gold-dim` はボタン・バッジのグラデーション背景にも使われているため直接上書きせず、テキスト用途専用のエイリアス`--gold-text` / `--gold-text-soft` / `--gold-text-dim`を新設（デフォルトは元の値と同じ、`.section-light`内でのみ白背景でも読みやすい濃いブロンズ色に上書き）。ボタン・バッジのグラデーション自体はセクションに関わらず常に同じ見た目のまま
- `--trust`（リンク・TELバッジの水色）は他に背景グラデーション用途がないため直接上書き
- ヒーローの「受電対応画面」モックアップ（`.console`まわり）は、セクションが白でも常に黒いデバイス画面のまま見せたいため、色をCSS変数依存ではなくハードコード（ダークテーマの元の値）に変更し、セクションのテーマ切り替えの影響を受けないようにした
- Problem セクションの3つのアイコン（stroke色）のみ、白背景での視認性のため`#c9a24b`から`#9c7a2e`（濃いブロンズ）に変更。Trust セクションの2アイコンは黒背景のままなので変更なし

**発生した不具合と修正**: `.hero`クラスは、トップページの大きいヒーローと、`company.html`/`privacy.html`/`terms.html`/`contact.html`の`.doc-hero`（ページ見出し部分）で共用されていた。トップページ用に`.hero`へ直接白背景を追加した際、この共用に気づかず、他ページにも白背景だけが漏れて適用され、文字色（ダークテーマ用の白文字）はそのままだったため「白地に白文字」で読めなくなった（本人が実機で発見・報告）。修正: 白背景は`.hero.section-light`（トップページのみ）に限定し、`.hero`単体は元のダーク仕様に戻した。**教訓: 共有クラスに新しい背景色を追加する際は、そのクラスがどのページ・要素で再利用されているか必ず`grep`で確認すること**

## LPの信頼要素について（重要・本人合意済みの判断）

一度、本人から「導入実績10社」「メンエススタッフ在籍10年の経験」といった**事実でない**実績・経歴をLPに載せてほしいという依頼があったが、景品表示法（優良誤認表示）のリスクと実務上の信用リスクを説明して見送った。本人は納得のうえで「事実に基づいた信頼要素」への差し替えを依頼し、以下の4点を実施:

1. 代表自らが電話対応を行う旨（事実）
2. yoin運営元が開発している旨（事実、実際にyoinとの関連会社）
3. 「提供開始記念価格・先着5社様限定」の価格訴求（誇張ではなく実際の立ち上げ初期であることに基づく）
4. 「契約後30日間はいつでも解約可能」の保証（**本人が実際に運用でコミットする必要がある業務ポリシー**。営業を始める前に、この条件で問題ないか本人に最終確認すること）

今後もLPに実績・経歴を追加する場合は、事実に基づくもの以外は掲載しないこと。

**バックエンド（Vercel Serverless Functions）**
- `api/contact.js` … お問い合わせフォームの送信先。Resend経由でメール通知。**動作確認済み**（本人が実際に送信・受信を確認）。**2026年9月23日、通知先を`yufami601@gmail.com`に変更**（`NOTIFY_EMAIL`環境変数で上書き可、未設定時のデフォルト）。送信元（`from`）はResend側で検証済みのドメインが必要なため`support@yoin.jp`のまま固定（`FROM_EMAIL`環境変数で上書き可）。**注意**: Vercel側に`NOTIFY_EMAIL`環境変数が既に設定されている場合はそちらが優先されるため、このコード変更だけでは反映されない可能性がある。本人に環境変数の設定状況を確認してもらうこと
- `api/line-webhook.js` … LINE自動受付プランの本体。LINE Messaging APIのWebhookを受け、Claude（Haiku）で店舗のFAQ設定（`lib/salon-config.js`）に基づいて自動応答。**2026年9月8日、「AIが予約内容を整形→人間が最終確定」の半自動フローを実装済み**（詳細は下記「LINE自動受付プラン：予約整形・引き継ぎの実装」参照）。署名検証・Anthropic API失敗時のgraceful fallback・Redis未設定時のno-opフォールバックはローカルのモックテストで検証済み。**実際のLINE公式アカウントとの接続、および実際のClaude API呼び出しはまだ未実施**（LINEチャネルシークレット・アクセストークン・ANTHROPIC_API_KEYが必要。このサンドボックス環境は`api.anthropic.com`・`api.line.me`ともネットワーク許可リスト外のため、たとえキーがあってもこのセッションからは実APIへの疎通確認ができない）
- `lib/salon-config.js` … デモ店舗1店舗分のFAQ情報をハードコード。複数店舗展開時はチャネルIDごとに設定を切り替える設計に拡張が必要。`reservationHandoff.notifyEmail`（予約引き継ぎ通知の送信先）を追加
- `lib/conversation-store.js` … LINEユーザーごとの会話履歴をUpstash Redis（Vercel Marketplaceの「Redis」インテグレーション）に保存するモジュール。未設定でも動作は壊れず、履歴なしのステートレス応答にフォールバックする

## LINE自動受付プラン：予約整形・引き継ぎの実装（2026年9月8日）

上記「LINE自動受付プランのアーキテクチャ方針」で決めた「AIが予約内容の整形・引き継ぎまでを担い、最終確定は人間が行う」を`api/line-webhook.js`に実装した。

**実装内容**:
- Claudeのtool use機能で`submit_reservation_request`ツールを定義。コース・希望日時の両方がお客様とのやり取りから確認できたら、AIがこのツールを呼び出す設計（呼び出し＝予約確定ではなく、あくまで人間への引き継ぎトリガー）
- system promptに**「予約が確定した」という趣旨の表現を絶対に使わないこと**を明記し、必ず「担当より確認のうえ、追ってご連絡します」という趣旨で返信するよう指示（テストで実際にこの制約が守られることを確認済み、下記参照）
- ツール呼び出しが発生したら、Resend経由で担当者（`salonConfig.reservationHandoff.notifyEmail`、既定は`support@yoin.jp`、`RESERVATION_NOTIFY_EMAIL`環境変数で上書き可）へ、コース・希望日時・特記事項・LINEユーザーIDを記載したメールを送信。メール本文にも「最終確定は担当者が行ってください」と明記
- 1回のLINEメッセージだけでコース・日時が揃わないケースに対応するため、`lib/conversation-store.js`でユーザーごとの直近12ターンの会話履歴をUpstash Redisに30分TTLで保存し、次のメッセージ受信時にその履歴も含めてClaudeに渡す。引き継ぎが完了したら履歴をクリアする
- Redis未設定時は履歴なし（＝改修前と同じ単発応答）に自動フォールバックするため、Upstash側の設定が未完了でもデプロイ自体は壊れない

**ローカルモックテストで確認済み**（`/home/user/linebot`環境、Anthropic/Resend SDKをモック化して検証。このセッションからは実APIへの疎通ができないため、あくまでロジックの正しさの検証）:
- 署名検証（正しい署名→200、不正な署名→401）
- Anthropic API呼び出し失敗時に、LINEへフォールバックメッセージを送ろうとして（実際には失敗するが）例外を握りつぶし、webhook全体としては200を返す
- ツール呼び出しが発生した際、実際に引き継ぎメールへコース・希望日時・LINEユーザーIDが正しく渡ること、メール本文に「最終確定は担当者」の注記が含まれること
- ツール呼び出し発生時のLINE返信文言に「確定」「承りました」等の確定を意味する語が含まれないこと

**未実施・要本人対応**:
- Upstash Redis（Vercel Marketplace経由）のセットアップと、`UPSTASH_REDIS_REST_URL` / `UPSTASH_REDIS_REST_TOKEN`（または`KV_REST_API_URL` / `KV_REST_API_TOKEN`）のVercel環境変数設定。未設定でも動くが、複数メッセージにまたがる予約希望の聞き取りができない
- `RESERVATION_NOTIFY_EMAIL`環境変数の設定（未設定時は`support@yoin.jp`宛のまま）
- 上記「残っている作業」記載の通り、LINE公式アカウントとの実接続・`ANTHROPIC_API_KEY`設定はまだ
- `npm install`で`package.json`に`@upstash/redis`を追加済み（旧`@vercel/kv`は2025年に非推奨化されているため使用していない。`npm install`時にその旨の警告が出ることに注意されたいが、これは意図的にavoidした結果であり問題ない）

## 料金

**電話受け代行プラン: 成功報酬型に変更済み（2026年7月）**
- 初期費用 ¥0・月額固定費 ¥0・成約1件 ¥990（税込）〜
- 対応時間帯（24時間365日 or 時間帯限定）は契約時に選択、単価は共通
- ライト/スタンダードの2段階プランは廃止し、単一プランに統一
- 根拠: 汎用の電話代行/秘書代行（月額固定・コール課金型、相場100〜300円/件）ではなく、**メンズエステ専門の電話受付代行業者**（成約報酬型が主流）で比較。実勢は800〜1,100円/成約（タイガーコール800円、代行.COM/Mint 1,000円、MTS 1,100円、契約店舗平均の一例で約763円）。990円はそのレンジの中央〜やや下（WebSearchで調査済み、出典はセッション内会話に記載）

| プラン | ライト | スタンダード |
|---|---|---|
| LINE自動受付（変更なし） | ¥29,000/月〜 | ¥49,000/月〜 |

複数店舗・両プラン併用は個別見積もり。

**経営サポートオプション（月額、2026年7月追加）**

| 項目 | 料金 |
|---|---|
| 基本料金 | ¥18,000（電話受け代行プラン利用中の店舗は無料） |
| 女子給精算代行 | ¥18,000〜 |
| シフト作成・管理 | ¥13,000〜 |
| SNS更新代行 | ¥18,000〜 |
| 広告更新代行 | ¥22,000〜 |

根拠: 競合「クラウド店長（Parallel）」の実際の料金ページ（本人がスクリーンショットで共有、女子給精算¥20,000〜・シフト管理¥15,000〜・SNS更新¥20,000〜・広告更新¥25,000〜・基本料金¥20,000＝電話代行利用者は無料）を基準に、10%引き程度で設定。

**要フォローアップ**: 業務委託契約書ひな形（`yoin-desk-service-agreement.docx`）の第2条（業務内容）は、現状「電話応対及び予約受付等の事務代行業務」のみを対象としており、経営サポートオプション（特に女子給精算のような金銭を扱う業務）はスコープに含まれていない。この新オプションを実際に契約書ベースで提供する場合は、契約書の業務内容・免責条項を拡張する必要がある（未対応）。

**「成約」の定義（本人確定済み・2026年7月）**: 予約確定時点ではなく、**実際のご来店・施術完了**をもってカウント（予約のみ・キャンセル・不成立は請求対象外）。LP・営業用ワンページャー・オンボーディングシートに反映済み。

**運用上の注意点**: 施術完了ベースの請求のため、YOIN DESK側は電話対応をしても実際に来店したかどうかを自分では確認できない。オンボーディングシートに「6. 予約の取り次ぎ方法」の下へ「成約（ご来店・施術完了）の報告方法・タイミング」の項目を追加し、店舗側から都度報告してもらう運用にした。ただし報告が遅れる・漏れる店舗が出た場合の請求金額の確定方法（店舗報告ベースで確定とするか等）は、まだ利用規約・契約書レベルでは明文化していない。契約書のひな形を作る際に要対応。

## 定款について（重要・本人合意済みの判断）

合同会社グラステの定款はイベント・エンタメ・飲食店運営中心の文言で、この「受付代行（BPO/コールセンター業務）」は**現状の14項目のどれにも該当しない**（yoinの時と違い、こじつけでカバーできる項目がない）。

本人と協議の結果、**今は定款変更をせずにそのまま事業を開始する方針**で合意済み（2026年7月時点）。ただし以下のタイミングまでには定款変更（目的追加登記、登録免許税3万円程度、自分で法務局手続き可能）を済ませることを推奨として伝えてある：

- 最初の実契約が決まりそうなタイミング
- このサービス用にStripe等の決済・本人確認を進めるタイミング

追加すべき文言案（未実施）:
```
電話・オンラインチャット等による受付代行、コールセンター業務の企画、運営及び受託
```

## Vercelデプロイ設定について（2026年7月、解決済み）

`yoin-desk.vercel.app` にアクセスしても古い別プロトタイプ（Next.js製のLINEボットデモ、ブランチ`claude/japanese-conversation-b5f68u`）が表示される問題が発生していた。

- 第一の原因: このリポジトリのGitHubデフォルトブランチが`main`ではなく`claude/japanese-conversation-b5f68u`になっていた。本人がGitHub側でデフォルトブランチを`main`に変更済み
- 真の原因（こちらが本体）: Vercelプロジェクトの「Framework Preset」が、当初のNext.jsプロトタイプ作成時のまま`Next.js`に固定されていた。`main`は素のHTML/CSS+Serverless Functions構成でNext.jsではないため、`next build`を試みて毎回ビルドが失敗（"No Next.js version detected"エラー）していた
- 対応: 新規importした`linebot`プロジェクト（本番URL `https://linebot-iota-ashy.vercel.app`）でSettings → Build and DeploymentのFramework Presetを`Other`に変更 → Redeployで解決・Ready確認済み。**以後はこの`linebot`プロジェクトを正とする**（元の`yoin-desk`プロジェクトは同じ原因で未修正のまま放置でよいと本人合意済み、混乱防止のため将来的に削除してもよい）

## LP軽量化・アクセス解析（2026年7月）

営業の反応が薄いことから「LPが長すぎるのでは」という懸念が本人から出た。実データはまだ無いが（アクセス解析未導入だったため）、以下を実施:
- Pricingセクション内、まだ販売していない「LINE自動受付プラン」のカードを、フルサイズ2枚（ライト/スタンダード）から1行の説明文＋ボタンに圧縮し、スクロール量を削減
- 全ページ（`index.html`/`company.html`/`privacy.html`/`terms.html`/`contact.html`/`thanks.html`）に Vercel Analytics のスクリプトタグ（`<script defer src="/_vercel/insights/script.js"></script>`）を追加済み
- **要本人対応**: Vercelダッシュボード → `linebot`プロジェクト → 「Analytics」タブで Web Analytics を有効化しないと計測が始まらない。有効化後、数日分データが貯まってから離脱状況を確認する想定

## 公式LINE（YOIN DESK自身の営業ツール、2026年7月設定）

「LINE自動受付プラン」（各店舗に導入する製品機能、上記の通り保留中）とは別に、**YOIN DESK自身の公式LINEアカウントを既存店への営業ツールとして運用開始**。

- プロフィール画像・あいさつメッセージ（友達追加時に3通に分けて自動送信）・リッチメニュー（2×2、`richmenu.png`）を設定済み
- リッチメニューの4エリア: サービス紹介(`/`) / 料金・成功報酬(`/#pricing`) / 導入シミュレーション(`/#simulation`) / 無料相談（テキスト送信で「無料相談を希望します」)
- **重要な制約**: LINE公式アカウントは「先に友達追加してくれた人」にしかメッセージを送れない仕組みのため、公式アカウントから見込み客への直接のコールドDMは不可能。営業の流れは「メール／Instagram DM等の別経路で最初の接点を作る → 興味を持った店舗に公式LINEを友達追加してもらう → そこから先はリッチメニュー・トークで対応」という設計にした
- 個人LINEアカウント経由の直接コールドメッセージ（求人サイト等に公開されているLINE IDに直接追加して送る方法。競合の一部が実際にこれをやっていると判明）は、当初は受け手の印象を害しやすいため非推奨と伝えたが、本人の判断で実施する方針に。新規に中古スマホ＋別回線を用意する予定だったが、**本業の会社支給端末（未使用）のLINEアカウントを転用する**ことで代替。表示名・プロフィール画像を「YOIN DESK」用に変更して使う想定（アイコン画像は下記参照）。会社支給端末の私的（副業）利用が就業規則に抵触しないか要本人確認、と伝達済み
- この用途向けの1通完結型コールド営業文面（求人サイト等で見つけたLINE IDへ直接送る想定、悩み喚起→特徴→シミュレーションリンク→オプトアウト一文の構成）も作成済み、本人にチャット上で送付済み（ファイル化はしていない）

## LPの導入シミュレーション表（`#simulation`）

料金セクションに、成約件数別の月額目安を表示する表を追加（`index.html` `#simulation`、`style.css` `.sim-table`）。1件¥990として算出。行の想定件数は本人指示で **1日10件・30件・50件** に設定（複数店舗・大規模運用も想定した数字）。

## 営業用に作成した補助ファイル（`/tmp`スクラッチパッド、リポジトリには未格納）

本人へ送付済み。リポジトリには含めていない（生成物のため）:
- 請求書テンプレート（`yoin-desk-invoice-template.docx`）: 成功報酬型（成約1件¥990〜）の銀行振込請求書。`〔　〕`部分は差し替え用プレースホルダー。**このセッションの環境ではLibreOfficeが動作せず視覚レンダリング確認ができていない**（XMLスキーマ検証のみ通過）。Wordで開いた際の体裁崩れがないか本人未確認
- 営業進捗管理シート（`yoin-desk-sales-tracker.xlsx`）: 店舗名・連絡先・接触経路・ステータス（ドロップダウン）等の進捗管理表。旧セッションのスクラッチパッドで作成したもので、コンテナ再起動により消失済み
- 営業DM文面（メール用・Instagram DM用・個人LINE直送用の3パターン）: 本人にチャット上で送付済み、ファイル化はしていない
- **営業リード（三宮エリア、2026年9月21日）**: `yoin-desk-sales-leads-sannomiya.xlsx`。公式サイト・求人サイトをWebSearchで個別確認し、5店舗（ヴィーナス神戸／FiveStar／Premium Rich／白ねこエステ／Process）の電話番号・公式サイトURLを掲載。**一斉スクレイピングではなく個別手動確認**（本人に既に伝えた「全国一斉のメール/電話/LINE収集はしない」方針を踏襲、地域を絞った少数精鋭リストとして継続予定）。メール・LINEは5店舗とも非公開で見つからず。掲載の電話番号は来店予約用の窓口である旨を明記し、営業電話の際の配慮を注記済み
- **業務委託契約書ひな形（`yoin-desk-service-agreement.docx`）**: 成功報酬型（成約990円/件）の電話受付代行契約書。本人の強い要望を踏まえ、以下の構成にした:
  - 業務範囲を「事務的な受付代行のみ」に限定し、性的サービスの案内・斡旋を含まないことを明記（第2条）
  - **甲（店舗側）に「風営法上の許可を要する営業でない」「売春防止法違反の性的サービスを提供していない」ことを表明保証させる条項（第3条）**＋違反が発覚した場合の即時解除・乙の免責（第4条）
  - 乙（YOIN DESK）の責任範囲を受付代行業務自体に限定する免責・賠償上限（第5条）
  - 反社会的勢力排除条項（第10条）
  - **重要な経緯**: 本人から当初「性的行為等をしていても一切法的責任を負わない旨を記載してほしい」という依頼があったが、契約書の一文で刑事責任（知りながら違法行為を幇助していた場合等）を免れることはできない旨を説明。本人は「知りながらやる可能性はない」と回答したため、実際に法的保護として機能する上記の表明保証・解除権・免責条項の構成で合意し作成。今後も、契約書に「知っていても免責」のような実効性のない文言を追加する依頼があった場合は、同様に説明して機能する代替案を提示すること
  - 弁護士レビュー前提のたたき台。第3〜5条・第10条は特に、実際の使用前に弁護士確認を強く推奨と伝達済み。LibreOffice未動作のため視覚レンダリング未確認（XMLスキーマ検証のみ通過）

## 現在の優先方針（2026年7月時点、本人の指示）

**LINE自動受付プランの接続作業は後回し。まず電話受け代行プラン（有人）を先に営業・展開できる状態にする方針に転換。**

- LINE Developersでの channel 作成は着手済みだったが（Messaging APIチャネル作成フローがLINE側の仕様変更でLINE公式アカウント経由に変わっていることを確認）、途中で一旦保留
- 保留中の状態: プロバイダー「YOIN DESK」は作成済みの可能性あり（本人の画面で確認）。Channel secret/access tokenの取得・Vercelへの環境変数設定・Webhook登録はまだ未実施
- 再開時は本CLAUDE.mdの「LINE公式アカウントとの実接続」手順（下記）から続きを行う

## 残っている作業

### 電話受け代行プラン（今の優先事項）
1. **営業用資料 作成済み・本人に送付済み**（`sales-materials/`配下）:
   - `phone-plan-onepager.html` … 営業用A4一枚資料のソース（PDF化はPlaywrightのheadless Chromiumで印刷。このリポジトリにはPDF本体は含めていない、ビルド成果物のため）
   - `generate-onboarding-sheet.js` / `generate-talk-script.js` … docx-js生成スクリプト。実行するとクライアントオンボーディングシート／トークスクリプトのWord文書が生成される
   - 注意: このセッションの環境ではLibreOffice経由のPDF変換ができず（環境側の制約）、生成したdocxは自前のXMLスキーマ検証(`validate.py`)とテキスト抽出で内容確認済みだが、レイアウトの見た目は本人未確認。Word/Google Docsで開いた際に体裁が崩れていないか要確認
2. 実際の電話受付体制の構築（本人自身が最初のオペレーターとして対応するか、採用するか、コールセンターBPO会社と提携するか）— 本人の事業判断、コードでは解決しない
   - **電話環境の技術構成が判明（2026年9月21日）**: 受電側は楽天コネクトSmaCom（クラウドPBX）、オペレーターの応対はRecTEL2（レクテル2、CBA社のZoiper5ベースの録音対応ソフトフォン）を使う構成。店舗の電話番号→SmaComへ転送→RecTEL2でオペレーターが応答、という流れ。この構成をもとにした初期設定マニュアルのたたき台（`yoin-desk-phone-setup-manual.docx`、本人にファイル納品済み・リポジトリ非格納）を作成
   - **SmaComの外部SIP対応をWebSearchで確認済み（2026年9月21日）**: SmaComは外部SIPソフトフォン（RecTEL2等）からの接続に対応しており、内線ユーザーごとに「ユーザー番号」「SIP用パスワード」が発行される。アウトバウンドプロキシは`sip.connectsmacom.comm.rakuten.net`、SIP/TLS・UDP 10000〜40000・RTP/SRTP対応（WebRTC利用時はTCP 7443）。マニュアルのSTEP2/STEP3に反映済み。**残るのはSmaCom管理画面で内線ユーザーを作成する具体的なクリック手順のみ**（実際のスクリーンショットが必要、未確認のプレースホルダーのまま）
3. 電話番号の用意（既存の電話番号を使うか、新規に転送用の番号を用意するか）— 本人の対応事項
4. **営業活動を開始（2026年7月末〜8月、本人の指示で進行中）**: メール／Instagram DMでの新規営業、公式LINEの友達追加誘導。個人LINEアカウント経由の直接営業も検討したが、中古スマホ＋別電話番号の調達が必要なため後回し中
5. 銀行振込用の送信元メールアドレス（Gmail等、事務連絡・返信用）の取得 — 本人対応事項、未確認
6. 契約書ひな形（「成約」の判定に本人・店舗間で認識ズレが起きた場合の扱いを明文化する必要あり、上記参照）— 未着手

### LINE自動受付プラン（保留中、いずれ再開）
4. **LINE公式アカウントとの実接続**: LINE Developersでチャネル作成 → `LINE_CHANNEL_SECRET` / `LINE_CHANNEL_ACCESS_TOKEN` をVercelに設定 → Webhook URL（`https://<デプロイURL>/api/line-webhook`）を登録
5. **`ANTHROPIC_API_KEY`をVercelに設定**（LINEボットの応答生成に必要）
6. **複数店舗対応の設計**: 現状`lib/salon-config.js`は1店舗分のみ。実際に複数店舗へ展開する際は、LINEチャネルIDごとに設定を出し分ける仕組みが必要
7. ~~予約整形・引き継ぎロジックの実装~~ → **2026年9月8日、実装済み**（詳細は上記「LINE自動受付プラン：予約整形・引き継ぎの実装」参照）。残るのはUpstash Redis・`RESERVATION_NOTIFY_EMAIL`のセットアップと、上記4・5の実接続作業

### 共通
7. 定款変更（本人が任意のタイミングで実施、上記参照）
