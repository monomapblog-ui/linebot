# Visual Design System (DESIGN.md)

本ファイルは、AIエージェントがUIコード（HTML、CSSなど）を生成する際に参照するデザイン仕様書です。YOIN DESK（メンズエステ専門受付代行サービス）のサイトを構築・改修する際は、必ずこのルールに従ってください。

## 1. トーン&マナー

* **ブランドイメージ:** 信頼できるプロフェッショナル × 言い切る力強さ。「よくある電話代行」の生真面目・お堅い印象ではなく、課題をズバッと言語化する鋭さと親しみやすさを両立する
* **コアバリュー:** 「もう、電話代行に振り回されない。」— 既存の電話代行・BPOサービスに対する明確な優位性・脱却を訴求すること
* **やってはいけないこと:** 官公庁的な堅すぎる文体、過度な絵文字の乱用、根拠のない誇大表現（優良誤認表示に注意）

## 2. カラーパレット (Colors)

指定された役割（トークン）に応じて、以下のカラーコードを厳密に使用してください。ライトセクション／ダークセクションのハイブリッド構成（`.section-light`で切り替え）は現行サイトの仕組みを継続します。

* **Primary (ブランドカラー・信頼):** `#1E4FD6` （例: コーポレートブルー。CTA以外の信頼訴求・リンク・アイコンに使用）
* **Secondary (アクセント・こだわり):** `#C9A24B` （例: ゴールド。既存ブランドから継承する高級感・専門性の差し色。バッジ・見出し装飾・CTAボタンに使用）
* **Dark Base (ダークセクション背景):** `#0F1730` （例: ネイビー。ブランドの強み・こだわりセクション等、インパクトを出したい箇所の背景）
* **Background (ライトセクション背景):** `#F7F8FA` （例: オフホワイト。課題・料金・導入の流れ等、読みやすさ優先の箇所）
* **Surface (カード背景):** `#FFFFFF` （例: ライトセクション上のカード・テーブル背景）
* **Text Main (本文テキスト・ライト面):** `#111827` （例: チャコールブラック）
* **Text Main (本文テキスト・ダーク面):** `#F5F6F8` （例: オフホワイト）
* **Text Muted (薄いテキスト):** `#6B7280` （例: グレー。ライト面用。ダーク面では`#9CA3AF`相当を使用）
* **Border (境界線):** `#E5E7EB` （例: ライトグレー。ダーク面では`rgba(255,255,255,0.12)`相当）
* **Highlight Marker (見出し強調):** `#FFE066` （例: マーカーイエロー。見出し内の重要フレーズに下線・背景として使用。多用しない）
* **Success/Badge (社会的証明):** `#10B981` （例: 導入実績・信頼バッジの補助色。使用は最小限に）

## 3. タイポグラフィ (Typography)

* **フォントファミリー（日本語）:** `"Noto Sans JP", "Hiragino Sans", "Yu Gothic", sans-serif`
* **フォントファミリー（英数字）:** `"Inter", system-ui, -apple-system, sans-serif`
* **ベースフォントサイズ:** `16px` (1rem)
* **見出し1 (h1・ヒーロー用):** `clamp(2rem, 5vw, 3.5rem)`、`line-height: 1.25`、太字（900）。1行に収まる短い言い切りコピーを想定
* **見出し2 (h2・セクション見出し):** `clamp(1.5rem, 3vw, 2.25rem)`、`line-height: 1.35`、太字（800）
* **見出し3 (h3・カード見出し等):** `1.25rem` (20px)、`line-height: 1.4`、太字（700）
* **本文 (p):** `1rem` (16px)、`line-height: 1.7`（日本語の可読性を優先し標準よりやや広め）
* **キャッチコピー装飾:** 見出し内の重要な語句のみ、Highlight Markerの下線／背景で強調（BIZTELの手法を参照）。1見出しにつき1箇所まで

## 4. スペースとレイアウト (Spacing)

余白や配置は、以下の8pxグリッドシステム（ベースは4px）を基準に設定してください。

* `spacing-0` (最小の余白): `4px`
* `spacing-1` (要素内の余白): `8px`
* `spacing-2` (標準的な余白): `16px`
* `spacing-3` (コンポーネント間): `24px`
* `spacing-4` (セクション内ブロック間): `40px`
* `spacing-5` (セクション間の余白): `96px`（モバイルは`64px`）
* **レイアウト方式:** CSS Flexbox または CSS Grid を使用する。1カラムのコンテンツ幅は最大`1120px`、`padding-inline: 24px`を基準とする
* **セクション構成:** ライト面とダーク面を交互に配置し、視線の切れ目でセクションの役割が変わったことを直感的に伝える（現行の`.section-light`スコープ方式を継続）

## 5. コンポーネント (Components)

* **ボタン（Primary CTA）:** 背景`#C9A24B`（ゴールド）、文字色`#111827`、`border-radius: 12px`、`padding: 16px 32px`、太字。hoverで明度を+8%
* **ボタン（Secondary）:** 背景透明、`border: 1.5px solid #1E4FD6`、文字色`#1E4FD6`
* **バッジ（社会的証明・実績訴求）:** 角丸`999px`のピル型、背景`rgba(30,79,214,0.08)`、文字色`#1E4FD6`、極小の太字ラベル（例:「導入実績」「対応件数」等、事実に基づく数値のみ使用。BIZTELの「国内シェアNo.1」のような誇大バッジは根拠なく模倣しない）
* **カード:** `border-radius: 16px`、`box-shadow: 0 4px 24px rgba(0,0,0,0.06)`（ダーク面では`border: 1px solid rgba(255,255,255,0.1)`に置き換え、シャドウは使わない）
* **アコーディオン:** 既存の`<details>/<summary>`ゼロJS実装を継続。見出し右端に開閉アイコン
* **吹き出し／会話UI:** LINE受付イメージや導入事例紹介など、対話を見せたい箇所限定でくまWEB的な吹き出しコンポーネントを使用可（多用しない）

## 6. 画像・アイコン方針 (Imagery)

* **人物写真:** 「実在の担当者が対応している」安心感を演出する用途に限定し、AI生成イラストや過度に演出的な写真（ロボット/コールセンター感の強い画像）は避ける
* **アイコン:** 線幅`1.5px`のラインアイコンを基本とし、色は`Primary`または`Text Muted`のみ使用。ダーク面では`Secondary（ゴールド）`も許可
* **図解:** 料金シミュレーション・導入フローなど数値/手順を伝える箇所では、装飾より情報伝達を優先したシンプルな図解を用いる

## 7. コピーライティングトーン (Voice)

* **見出しコピー:** 短く言い切る。課題を名指しし、そこからの脱却を明言する構成（例:「もう、電話代行に振り回されない。」）。fondeskのような言葉遊び・ユーモアは、ブランドの信頼性を損なわない範囲でサブコピーや遊び要素のあるセクションに限定して使用
* **本文コピー:** 誠実さを優先し、断定表現は根拠のある事実（実績・料金・仕組み）にのみ使用する
* **CTAコピー:** 「相談する」「見積もりを見る」等、次の行動が明確な動詞で終える
